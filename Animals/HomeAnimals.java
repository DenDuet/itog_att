import java.time.LocalDate;
import java.util.ArrayList;

public class HomeAnimals extends Animal {

    public HomeAnimals(String name, LocalDate birthday, ArrayList<String> commands) {
        super(name, birthday, commands);
    }
}
